
# This file was generated by NICS (3.0.0b5).
# Documentation: https://github.com/nvfp/now-i-can-sleep

name: Rebuild documentation

on:
  push:
    branches:
      - main

    paths:
      - 'nics-docs/**'  # Only rebuild if 'nics-docs/' folder is modified

      # When using 'use-repo-readme' keyword in 'tree/index.md'
      - README.md
      - readme.md

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  NICS-rebuild:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Run
        uses: nvfp/now-i-can-sleep@3.0.0b5
        with:
          load: main
          dock: nics-docs
          container: nics-docs
          git-name: by NICS
          git-email: nicho@scapeville.com
          gh-repo-name: demo-testing
          nics-version: 3.0.0b5
